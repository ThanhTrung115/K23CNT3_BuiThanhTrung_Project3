# Server
server.port=8080

# Datasource (MySQL)
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/treeree_shop?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Merinos104

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.thymeleaf.cache=false